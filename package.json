{
  "name": "opal",
  "version": "0.0.1",
  "private": true,

  "dependencies": {
    "node-outlook": "1.1.4"
  },
  "devDependencies": {
    "tape": "^4.2.2",
    "faucet": "^0.0.1",
    "typescript": "^1.7.3",
    "tsd": "^0.6.5"
  },

  "scripts": {
    "build": "tsc",
    "smoketest": "node opal.js",

    "pretest": "node test/tsconfig.json",
    "test": "node test.js | faucet"
  }
}
